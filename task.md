# 남은 작업

> **갱신** 2026-09-10 · 이 파일은 **남은 것만** 적는다. 끝난 항목은 지운다 — 이력은 [`docs/roadmap.md`](docs/roadmap.md) §3에 있다.

---

## 0. Firestore 쿼터 — ✅ 해결 (2026-09-10)

원인이던 **부팅 읽기를 제거**했고(로드당 API 요청 0건), 한도는 **16:05 KST에 초기화**됐다(예측 16:00 = 태평양 자정, 5분 간격 폴링으로 확인).

| 확인 | 결과 |
|---|---|
| `/api/data/summary` | **200** · `492건` · `2025-03-01 ~ 2026-06-01` · 추세 `상승` |
| 채팅 1회 왕복 | **200** · `prompt 1,706 / completion 15` |
| 기준 상태 복구 | **conversations 0 / data 492** — 검증 중 생긴 대화 2건 삭제 |

> 재발 방지는 코드에 있다(`ensureDataTable` · `loadSidePanel`). 같은 실수를 막는 규칙은 §3의 *"읽기도 비용이다"*.

---

## 1. 다크 모드 (A32) — 스펙만, 미구현

- [ ] **다크 모드** (M, P2) — 스펙 [`A32`](docs/specs/A-product-ux.md) · 값과 상세는 [`design-system.md`](docs/design/design-system.md) §5

  **토큰 교체 작업이다.** A15 덕에 색 리터럴 43개가 전부 `:root` 안에 있고 밖에는 0개다. `@media (prefers-color-scheme: dark)` 한 블록이면 대부분 끝난다. **토글은 만들지 않는다** — 헤더에 버튼이 셋이고 390px에서 제목을 말줄임해야 겨우 들어간다.

  **그냥 뒤집으면 깨지는 것 5가지가 이 작업의 실체다**:
  1. **지도 램프는 반전이 아니라 방향 재설정** — 지금은 값이 클수록 어둡다. 다크에서 그대로면 최고값이 배경에 가라앉아 읽기가 뒤집힌다
  2. **범주 팔레트(A26)** — 밝기가 밀도를 지므로 `촘촘함 = 밝음`으로. 색상 축은 그대로
  3. **`map.js`가 색을 시작할 때 한 번만 읽는다** — 테마가 도중에 바뀌면 지도만 옛 색으로 남는다
  4. **임베드 액자가 무너진다** — `--viewport-bg`가 다크 배경과 붙는다. 테두리로 액자 유지
  5. **그림자가 안 보인다** — 플로팅 패널 둘이 배경에 잠긴다. 테두리로 대신

  `color-scheme: light dark` 선언 필수 — 없으면 `<select>` 5개가 밝게 남는다.

  **끝났다고 볼 조건**: 다크에서 **Lighthouse 접근성 100**(라이트 통과가 다크를 보장하지 않는다 — A30에서 실제로 2건 걸렸다) · 지도에서 **값이 클수록 밝게** · **82개 동의 5구간 분포가 그대로**(경계는 색과 무관하므로 변하면 버그) · `<select>`·스크롤바까지 따라옴.

---

## 2. 참고 기록

### `main` 브랜치 보호 — ✅ 적용 (2026-09-10)

**"대시보드 작업이라 코드로 못 넣는다"는 틀린 메모였다.** 검증 없이 오래 옮겨 적혀 있었다 —
공개 저장소 + ADMIN + `repo` 스코프면 API로 된다.

```bash
gh api -X PUT repos/<owner>/<repo>/branches/main/protection --input prot.json
```

| 설정 | 값 | 왜 |
|---|---|---|
| `required_status_checks.contexts` | `frontend` · `backend` | 이 작업이 요구한 것 |
| `strict` | `false` | 1인 저장소에서 "머지 전 최신화 강제"는 마찰만 는다 |
| `enforce_admins` | **`false`** | **켜면 우리 배포 경로가 막힌다** — 로컬에서 머지해 `main`에 직접 푸시하는데, 그 SHA에는 체크가 아직 안 돌았다. 관리자는 통과시키고 PR 경로만 게이트한다 |

---

## 3. 안 하기로 한 것 (기록만, 되살릴 조건 포함)

| | 판단 | 되살릴 조건 |
|---|---|---|
| **E3 데모 녹화** | ⛔ **사용자 생략 결정** (2026-09-09) | 대본은 [`docs/DEMO.md`](docs/DEMO.md)에 그대로 있다 |
| **B5 insights RAG** | ⛔ **하면 손해** — 캐시 프리픽스가 2,460자 → 650자로 줄어 1,024토큰 임계 미달, 순이득 약 180토큰 | 리포트가 **4,000자를 넘으면** 다시 본다 |
| **C7 프론트 에러 트래킹** | ⛔ **안 붙인다** (2026-09-10) — 빌드가 없어 번들에 섞을 수 없고, 지도의 "외부 소스 0" 원칙과 부딪히고, 진짜 오류가 나는 API 경계는 백엔드가 이미 잡는다. 근거는 [`decisions.md`](docs/decisions.md) | 백엔드 로그로 설명되지 않는 화면 오류 신고가 실제로 들어올 때 |
| **A4a 답변 품질** | 🕐 **사용자가 향후 개선으로 미룸** | 마크다운 렌더는 들어갔다. 남은 것은 문단 길이·수치 인용 밀도·반복 |

---

## 4. 지켜야 하는 것

- `.env` · `firebase-service-account.json` — **커밋 금지, 값을 문서·로그·대화에 노출 금지**
- **워밍업·상태 확인에 쓰기 요청을 폴링하지 않는다** (Firestore 쓰레기 레코드 전례)
- 테스트는 Firestore·OpenAI를 모킹한다 — **실제 외부 호출 금지**
- 화면에 나가는 모든 문구는 한국어
- 검증 전에는 roadmap에서 완료로 올리지 않는다
- 수동 점검 뒤 기준 상태 복구: **conversations 0 / data 492**
- **읽기도 비용이다** — 2026-09-10 소진이 그 증거다. 목록을 통째로 읽는 코드를 새로 넣지 않는다
- **화면에서 잰다** — 스크린샷이 아니라 `getBoundingClientRect`·CSSOM 전수 조회로. 그리고 **`ignoreCache`로 새로고침한다** — 캐시된 옛 스크립트를 보고 "코드가 안 먹는다"고 오진한 적이 있다
- **규칙은 무대를 옮길 때 따라오지 않는다** — A15·A16이 그랬다. 새 무대를 만들면 [`design-system.md`](docs/design/design-system.md) §2의 "Display가 앉을 자리" 표에 줄을 추가한다
