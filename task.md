# 남은 작업

> **갱신** 2026-09-10 · **브랜치** `main` = `7c0bf6d` (푸시·CI 통과·Vercel 반영 완료)
>
> 이 파일은 **남은 것만** 적는다. 끝난 항목은 지운다 — 이력은 [`docs/roadmap.md`](docs/roadmap.md) §3에 있다.

---

## 0. 지금 막힌 것 — Firestore 일일 쿼터 소진 🔴

배포본과 로컬 **둘 다** `/api/data/summary`가 **500**이다. 지도는 로컬 JSON이라 뜨지만 요약·데이터 표·채팅이 죽어 있다.

```
google.api_core.exceptions.ResourceExhausted: 429 Quota exceeded
```

| | |
|---|---|
| 초기화 | **2026-09-10 16:00 KST** (= 태평양 자정) |
| 돌아오는 한도 | 무료 티어 하루 읽기 5만 / 쓰기 2만 / 삭제 2만 |

**무엇이 태웠나**: D3 리셋 한 번이 삭제 492 + 쓰기 492다. 거기에 오늘 검증하느라 페이지를 수십 번 열었다. 하지만 그건 계기고, 구조가 따로 있다.

- [ ] **페이지 로드당 492건 읽기를 없앤다** (S, P0)

  `frontend/js/history.js`의 부팅 블록이 `DOMContentLoaded`마다 `refreshDataTable()`을 부르고, 그게 `api.listData()` → `/api/data`로 **492개 문서를 통째로** 읽는다. 방문자 한 명당 약 500~1,000 reads라 **50~100명이면 하루치가 끝난다.**

  그런데 **데이터 표는 접혀 있는 `<details>` 안에 있다.** 열지도 않은 패널 때문에 매번 전량을 읽는 셈이다. `toggle` 이벤트에서 처음 열릴 때만 읽게 바꾼다. C5 캐시는 요약만 덮고 목록은 안 덮는다.

  **끝났다고 볼 조건**: 첫 로드 네트워크 탭에 `/api/data` 요청이 없고, `<details>`를 열면 그때 한 번 나간다.

- [ ] **16:00 이후 정상 동작 확인** — `/api/data/summary` 200, 채팅 1회 왕복, 기준 상태 `conversations 0 / data 492`

> ⚠️ 지금 상태로 4시를 맞으면 **새 한도도 같은 속도로 탄다.** 위 수정을 먼저 넣고 열리는 것을 맞는 순서다.

---

## 1. 지도 무대에 수치 타이포 위계를 준다 (A31)

- [ ] **선택한 동의 값을 화면의 주인공으로 세운다** (S, **P1**) — 스펙 [`A31`](docs/specs/A-product-ux.md) · [`design-system.md`](docs/design/design-system.md) §2

  **새 디자인 방향이 아니다. 이미 정한 것이 새 주무대로 따라오지 않았다.** A15의 완료 조건이 *"핵심 수치가 가장 크게 읽힌다"*, A16이 *"`tabular-nums`를 헤드라인 수치·요약·표로 확대"* 인데, 둘 다 **주무대가 주제 패널이던 시절에 검증돼 닫혔다.** A22·A27이 주무대를 지도로 옮길 때 함께 이사하지 않았다.

  **실측** — 첫 화면에 눈에 보이는 텍스트 전수(`<option>`·`sr-only` 제외):

  | 크기 | 개수 | 무엇 |
  |---|---|---|
  | 22px | 1 | `대전 상권분석 매니저` — **앱 이름** |
  | 18px | 2 | 패널 제목 |
  | 15px | 10 | 카드 제목·채팅 본문 |
  | 13px | 17 | 캡션·**범례** |
  | 11px | 26 | 지도 동 이름 |

  **가장 큰 글자가 앱 이름이고, 데이터 관련 최대치가 범례 13px이다.** 토큰의 `--fs-display`(28px)는 이 화면에 한 번도 안 쓰인다. 상권 수치를 파는 화면에서 수치가 UI 크롬과 같은 얼굴이다.

  추가로 `.map-legend`의 `font-variant-numeric`이 `normal`이다(`.map-selection`만 `tabular-nums`) — A16이 "전역"으로 적은 것과 어긋난다.

  **할 것**: ① 동을 고르면 나오는 값을 `--fs-display`로 세우고 기준선을 그 아래 Caption으로. 지금 `어느 동이 궁금하세요?`가 앉은 자리가 **이 제품에서 가장 중요한 숫자의 자리**다. ② 지도 무대 수치 전부 `tabular-nums`.

  **하지 말 것**: 서체 추가 · 700 웨이트 · 세리프 · 그림자 강조 — 전부 디자인 시스템에 금지 근거가 있다. **테두리 정리도 넣지 않는다** — 첫 화면에서 지도와 무게를 다투던 건 목적 블록 하나고 A30이 걷어낸다(사이드바 `.panel`은 기본 `display:none`이라 애초에 경쟁 안 함).

  **끝났다고 볼 조건**: 동을 고른 첫 화면에서 **가장 큰 글자가 앱 이름이 아니라 선택한 동의 수치** · 지도 무대 수치 전부 세로 정렬 · 서체 Pretendard 하나 유지 · Lighthouse 접근성 **100 유지**.

---

## 2. 목적 선택을 플로팅 패널로 옮긴다 (A30)

- [ ] **목적 카드를 `데이터·대화 기록`과 같은 패널로 이동** (M, P2) — 스펙 [`A30`](docs/specs/A-product-ux.md)

  **왜**: 첫 화면에 **질문 고르는 장치가 둘**이다 — 왼쪽 `무엇이 궁금하세요?` 카드 3장, 오른쪽 제안 질문 칩 3개. 둘 다 질문처럼 생겼는데 하는 일이 다르다(목적 설정 vs 질문 전송). 게다가 카드가 **첫 화면에서만** 지도를 눌러, 지도가 주역인데 첫인상의 지도가 가장 작다.

  | | 지도 높이 |
  |---|---|
  | 첫 방문 (카드 펼침) | **305px** |
  | 고른 뒤 (한 줄로 접힘) | **449px** |

  **모달을 새로 만드는 게 아니다.** `#pane-side`를 실측하니 backdrop 없음 · `aria-modal` 없음 · 뒤쪽 조작 가능 · Esc로 닫히고 포커스 복귀까지 이미 된다. **비모달 플로팅 패널**이라 A28의 `게이트로 만들지 않는다`와 충돌하지 않고 접근성 부채도 안 생긴다. `.data-open` 패턴(`fixed` · `right:16px` · 360px · `z-index:60` · `--shadow-float`)을 그대로 재사용한다.

  **못 박은 것 4가지** (스펙 A30):
  1. **첫 방문에만 자동 오픈** (`onboarded_v1`) — 안 열면 A28이 죽는다(안 보여서 승격한 건데 버튼 뒤로 숨기면 원위치)
  2. **기존 사이드 패널과 상호 배타** — 둘 다 `right:16px`·360px·`z-index:60`이라 동시에 열면 정확히 겹친다
  3. **모바일 하단 탭을 늘리지 않는다** — 헤더 토글을 좁은 화면에서도 살린다(A27이 정리한 것을 다시 어지럽히지 않는다)
  4. **`둘러보는 중 · 바꾸기` 한 줄(16px) 제거** — 현재 목적을 헤더 토글 라벨로(`목적: 둘러보는 중`)

  ⚠️ **빈 패널 주의** — 패널 793px에 카드 3장은 217px이라 4분의 3이 빈다. 높이를 내용에 맞추거나 *왜 고르나*로 채운다. **빈 채로 두면 지금보다 나쁜 인상을 준다** — 이 변경에서 유일하게 후퇴할 수 있는 지점이다.

  **끝났다고 볼 조건**: 첫 방문 시 패널이 열린 채 시작 · Esc로 닫으면 지도 **449px** · 재방문은 패널 없이 시작 · 두 패널 동시 오픈 불가 · 모바일 탭 3개 유지 · Lighthouse 접근성 **100 유지**.

---

## 3. 채팅 — 주제 전환 구분선이 쌓인다 (A29)

- [ ] **구분선을 새로 붙이지 말고 문구만 바꾼다** (S, P2) — 스펙 [`A29`](docs/specs/A-product-ux.md)

  `chat.js`의 `notifyTopicChange()`가 주제를 바꿀 때마다 `.topic-divider`를 **새로 append** 한다. 지금 있는 가드(`label === lastNotifiedTopic`)는 **같은 라벨 연속**만 막아서, 다른 주제로 옮기면 그대로 쌓인다.

  **실측** — 탭을 5번 누르니 말풍선 하나 없이 구분선만 5줄:

  ```
  주제를 '전체 규모·성장'(으)로 바꿨습니다
  주제를 '자치구별 증감'(으)로 바꿨습니다
  주제를 '전체 규모·성장'(으)로 바꿨습니다
  주제를 '자치구별 증감'(으)로 바꿨습니다
  주제를 '업종별 증감'(으)로 바꿨습니다
  ```

  **고칠 방향**: 직전 요소가 이미 구분선이면 **그 요소의 문구만 갈아 끼운다.** 사이에 말풍선이 들어갔다면 그 구분선은 진짜 대화 이력이니 건드리지 말고 새로 붙인다 — **지나간 지점을 나중에 고쳐 쓰면 이력이 거짓이 된다.** 주제 해제(`label === null`)도 같은 규칙이다.

  **끝났다고 볼 조건**: 연속 전환 5회 → 구분선 1줄, 문구는 마지막 주제. 질문 한 번 보낸 뒤 주제를 바꾸면 그때는 2줄.

---

## 4. 남은 자잘한 것 (5건)

- [ ] **F2 배포본 성능 재측정** (S) — 로컬만 쟀다(LCP 713ms / CLS 0.00). 배포본은 CDN·네트워크가 달라 값이 다르다
- [ ] **`favicon.ico` 없음** (XS) — 콘솔에 404. 파일 하나 넣으면 끝난다
- [ ] **C7 프론트 에러 트래킹 판단** (S) — 백엔드만 적용돼 있다. 정적 페이지에 외부 스크립트를 하나 더 붙일지가 판단 사항이다 (붙이면 F2·F1에 영향)
- [ ] **`↗` 링크 꼬리** (XS) — `전체 대시보드 ↗` · `원본 지도 확인 ↗`. 링크 텍스트에 화살표를 붙이는 것은 생성형 페이지의 흔한 흔적이다. 새 창으로 열린다는 신호는 필요하니 없애기보다 아이콘으로 분리할지 판단한다
- [ ] **`main` 브랜치 보호 규칙** — CI를 필수 체크로 지정. **대시보드 작업이라 코드로 못 넣는다** — 사람이 해야 한다

---

## 5. 안 하기로 한 것 (기록만, 되살릴 조건 포함)

| | 판단 | 되살릴 조건 |
|---|---|---|
| **E3 데모 녹화** | ⛔ **사용자 생략 결정** (2026-09-09) | 대본은 [`docs/DEMO.md`](docs/DEMO.md)에 그대로 있다. 마음이 바뀌면 따라 찍으면 된다 |
| **B5 insights RAG** | ⛔ **하면 손해** — 캐시 프리픽스가 2,460자 → 650자로 줄어 1,024토큰 임계 미달, 순이득 약 180토큰 | 리포트가 **4,000자를 넘으면** 다시 본다 |
| **A4a 답변 품질** | 🕐 **사용자가 향후 개선으로 미룸** | 마크다운 렌더는 들어갔다. 남은 것은 문단 길이·수치 인용 밀도·반복 |

---

## 6. 지켜야 하는 것

- `.env` · `firebase-service-account.json` — **커밋 금지, 값을 문서·로그·대화에 노출 금지**
- **워밍업·상태 확인에 쓰기 요청을 폴링하지 않는다** (Firestore 쓰레기 레코드 전례)
- 테스트는 Firestore·OpenAI를 모킹한다 — **실제 외부 호출 금지**
- 화면에 나가는 모든 문구는 한국어
- 검증 전에는 roadmap에서 완료로 올리지 않는다
- 수동 점검 뒤 기준 상태 복구: **conversations 0 / data 492**
- **읽기도 비용이다** — 이번 소진이 그 증거다. 목록을 통째로 읽는 코드를 새로 넣지 않는다
