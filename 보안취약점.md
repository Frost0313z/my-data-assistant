# 보안 취약점 체크리스트

> **작성** 2026-09-11 · **기준 커밋** `a914d99` · **범위** 이 저장소의 실제 스택
>
> 일반 OWASP 목록이 아니라 **이 코드에서 직접 확인한 것**만 적는다. 각 항목에 확인한 파일과 방법을 같이 둔다.
>
> `tech.md`는 저장소에 없어서 [`docs/context.md`](docs/context.md) §2와 코드에서 스택을 확인했다.

**스택** — FastAPI · Pydantic v2 · uvicorn · firebase-admin(Firestore) · OpenAI SDK · sentry-sdk / 바닐라 JS · MapLibre GL(CDN) / Render + Vercel

| 표시 | 뜻 |
|---|---|
| `[x]` ✅ | 막혀 있고 확인했다 |
| `[ ]` ⚠️ | 부분적이거나 조건부 — 판단이 필요하다 |
| `[ ]` ❌ | 안 되어 있다 |

---

## A. 인증 · 권한 — 이 스택에서 가장 큰 구멍

- [x] **1. ✅ 대화 기록이 전역 공개다** — **고침 (2026-09-11)**. 브라우저가 난수 `X-Client-Id`를 들고, 서버가 그 값으로 칸을 나눈다. 목록·조회·삭제·턴 추가 네 경로 전부 소유자를 확인하고, 남의 것은 403이 아니라 **404**로 답한다(403으로 갈라 주면 어떤 id가 실재하는지 알려 준다). 칸막이 회귀 테스트 4개 — 옛 동작에서 전부 실패함을 확인했다. → `routers/conversations.py`, `services/conversation_service.py`, `frontend/js/api.js`<br><br>**고치기 전**: `GET /api/conversations`가 모든 방문자의 대화를 돌려주고 `DELETE`에도 인증이 없었다. 목록이 곧 남의 대화를 찾는 경로였다 — 추측이 아니라 **2026-09-10에 다른 사람이 남긴 대화 4건이 실제로 목록에 보였다.**<br>⚠️ **부작용**: 예전 대화는 소유자 필드가 없어 목록에서 사라진다. 데이터는 Firestore에 그대로 있다.
- [x] **2. ✅ 데이터 쓰기 보호** — `DATA_WRITES_ENABLED` 기본 꺼짐. `POST/PUT/DELETE /api/data`가 403. 읽기는 열어 둔다. → `routers/data.py:require_writable`
- [x] **3. ✅ 데모 복구 엔드포인트** — `X-Dev-Token`을 `secrets.compare_digest`로 비교(타이밍 공격 방지), 토큰 미설정이면 404로 감춘다. → `routers/dev.py`
- [ ] **4. ⚠️ 사용자 인증 자체가 없다** — 의도적으로 범위 밖으로 둔 단일 사용자 데모다. 다만 **1번의 근본 원인**이므로, 공개 링크로 쓸 거면 최소한 대화 목록만이라도 가려야 한다.

## B. 남용 · 비용 — LLM 서비스 고유

- [x] **5. ✅ `X-Forwarded-For` 위조로 레이트리밋 우회** — 첫 항목은 클라이언트가 채운다. 신뢰 프록시가 붙이는 **오른쪽 끝**에서 `TRUSTED_PROXY_HOPS`만큼 센다. 위조 회귀 테스트 있음. → `routers/chat.py:_client_ip`
- [x] **6. ✅ 분당 요청 상한** — IP 단위 `CHAT_RATE_PER_MINUTE`(기본 10), 429 + `Retry-After`.
- [x] **7. ✅ 일일 토큰 예산 + 사전 예약** — 요청 시작 시 출력 상한 + 입력 추정치를 예약하고 완료 후 정산한다. 동시 요청이 예산을 넘지 못한다. → `ratelimit.py`
- [x] **8. ✅ 입력 길이 상한** — `message` 2,000자 · `memo` 500자 · `content` 8,000자 · context 값 80자. Pydantic에서 막는다. → `models.py`
- [x] **9. ✅ 스트리밍 엔드포인트도 같은 방어를 탄다** — `POST /api/chat/stream`도 `_guard`를 통과한다. (막지 않으면 우회로가 된다.)
- [x] **10. ✅ 읽기 폭증에 의한 비용 공격** — 부팅마다 492건을 읽던 구조를 제거했다(로드당 API 요청 0건). 2026-09-10에 실제로 하루 한도를 태운 전례가 있다.
- [ ] **11. ⚠️ 레이트리밋이 인메모리다** — 인스턴스를 늘리면 상한이 인스턴스 수만큼 느슨해진다. 지금은 1개라 유효하지만, 스케일아웃하는 순간 조용히 깨진다. → `ratelimit.py` 상단 주석에 명시됨

## C. 프롬프트 · LLM

- [ ] **12. ⚠️ 프롬프트 인젝션** — 사용자 텍스트(질문, `memo`, 대화 이력)가 프롬프트에 들어간다. *"이전 지시를 무시하고…"* 류를 막는 장치는 없다. **영향은 제한적이다** — 이 서비스는 도구 호출도 DB 쓰기도 시키지 않아서, 최악이 "이상한 답변"이다. 기능을 늘리기 전에 다시 본다.
- [x] **13. ✅ 화면 컨텍스트는 화이트리스트다** — `context`는 허용 키만 받고 값도 길이·문자 정리를 거친다. 임의 키가 프롬프트로 새지 않는다. → `models.py`
- [x] **14. ✅ 프롬프트에 비밀정보가 없다** — 시스템 프롬프트는 요약·`insights.md`·화면 컨텍스트 세 블록뿐이다. 키·토큰·서비스 계정이 들어가지 않는다.
- [ ] **15. ⚠️ 모델이 만든 숫자를 그대로 보여준다** — 보안보다 신뢰 문제지만 같이 본다. CI가 `insights.md`의 수치를 데이터와 대조하지만, **모델이 즉석에서 만든 수치**는 검증 경로가 없다.

## D. 웹 · 프론트엔드

- [ ] **16. ❌ CDN 스크립트에 SRI(`integrity`)가 없다** — `index.html`에 `integrity` 속성 **0개**. MapLibre GL(`unpkg`)은 페이지 권한으로 실행되는 스크립트라, unpkg가 뚫리거나 바뀌면 **전면 XSS**다. 버전은 고정(`5.6.0`)돼 있으나 그것만으로는 내용 변조를 못 막는다. → `frontend/index.html:213`
- [ ] **17. ❌ CSP(Content-Security-Policy)가 없다** — `vercel.json`에 `nosniff`와 `Referrer-Policy`만 있다. CSP가 있으면 16번의 피해를 크게 줄인다.
- [ ] **18. ❌ 클릭재킹 방어가 없다** — `X-Frame-Options` 또는 CSP `frame-ancestors`가 없어 아무 사이트나 이 앱을 iframe으로 감쌀 수 있다.
- [x] **19. ✅ MIME 스니핑 · 리퍼러** — `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`. → `frontend/vercel.json`
- [x] **20. ✅ 출력 XSS** — DOM 조립을 `createElement` + `textContent`로만 한다. 마크다운은 화이트리스트 렌더러이고, **CI가 공격 문자열 8종과 `innerHTML` 사용 금지를 검사**한다. → `frontend/scripts/check_markdown.js`
- [ ] **21. ⚠️ HSTS** — Vercel·Render가 HTTPS를 강제하지만 `Strict-Transport-Security` 헤더를 우리가 명시하진 않았다. 플랫폼 기본값을 확인하고, 없으면 `vercel.json`에 한 줄 넣는다.

> **16~18은 `vercel.json` 한 블록으로 같이 해결된다.** CSP를 넣을 때 `unpkg`·`jsdelivr`·`onrender.com`을 허용 목록에 넣어야 지도와 API가 죽지 않는다.

## E. CORS · 네트워크

- [ ] **22. ⚠️ `ALLOWED_ORIGINS` 기본값이 `*`인데 `allow_credentials=True`다** — 환경변수를 빼먹은 배포에서 위험한 조합이 된다. 기본을 좁히지 않은 것은 *"빠지면 프론트가 조용히 죽는다"* 는 의도적 선택이고 시작 로그에 경고를 남긴다. **지금은 쿠키·세션을 안 쓰므로 실제 피해는 낮다** — 인증을 붙이는 순간(4번) 즉시 위험해진다. → `backend/main.py`, `config.py`
- [ ] **23. ⚠️ `allow_methods=["*"]`, `allow_headers=["*"]`** — 필요한 것만 나열하는 편이 낫다. 우선순위는 낮다.

## F. 비밀정보 · 공급망

- [x] **24. ✅ `requirements.txt`에 버전이 하나도 고정돼 있지 않다** — **고침 (2026-09-11)**. 직접 의존성 6개 + 개발 의존성 2개를 `==`로 박았다(pytest 146개를 통과시킨 조합). 전이 의존성은 pip에 맡긴다 — Windows에서 뽑은 전체 lock은 리눅스에 없는 패키지가 섞여 Render 빌드를 깨뜨린다.<br><br>**고치기 전**: 이름만 적혀 있어 **Render가 재빌드할 때마다 그날의 최신**을 받았다. 어제 되던 게 오늘 깨질 수 있고, 업스트림이 오염되면 그대로 들어오고, 같은 커밋을 두 번 배포해도 같은 것이 올라간다는 보장이 없었다.<br>남은 것: 더 엄밀하게 가려면 리눅스에서 `pip-compile`로 전이 의존성까지 잠근다.
- [ ] **25. ❌ 의존성 취약점 스캔이 없다** — CI에 `pip-audit`이나 Dependabot이 없다. 24번을 고친 뒤 붙여야 의미가 있다.
- [x] **26. ✅ 프론트 외부 의존성 버전 고정** — `maplibre-gl@5.6.0`, `pretendard@v1.3.9`. (다만 SRI는 없다 — 16번)
- [x] **27. ✅ OpenAI 키가 프론트로 새지 않는다** — `build.js`가 주입하는 값은 `API_BASE_URL` 하나뿐이다. 키는 서버에만 있다.
- [x] **28. ✅ `.env`와 서비스 계정 파일이 gitignore돼 있다** — `backend/.gitignore`에 `.env`, `firebase-service-account.json`.
- [ ] **29. ⚠️ git 이력에 과거 유출이 없는지는 확인하지 않았다** — gitignore는 **지금부터**를 막을 뿐이다. `git log -p`나 gitleaks로 전체 이력을 한 번 훑는다.
- [ ] **30. ⚠️ 서비스 계정 키 = Firestore 전권** — 이 키 하나가 DB 전체를 연다. 최소 권한 역할로 좁히고 주기적으로 회전한다. 유출되면 데이터 삭제까지 가능하다.
- [x] **31. ✅ 클라이언트가 Firestore를 직접 부르지 않는다** — 프론트에 Firebase SDK가 없다. 모든 접근이 백엔드를 거치므로 클라이언트 측 보안 규칙 실수가 애초에 성립하지 않는다.

## G. 로깅 · 프라이버시

- [x] **32. ✅ 로그·Sentry에 질문 본문이 실리지 않는다** — `send_default_pii=False`에 더해 `message`·`reply`·`content`·`authorization`·`x-dev-token`을 지우는 필터가 한 겹 더 있다. → `observability.py:_SENSITIVE`
- [x] **33. ✅ 에러 응답에 스택트레이스가 나가지 않는다** — 트레이스는 서버 로그에만 남고 클라이언트는 표준 메시지를 받는다.
- [ ] **34. ⚠️ 대화 보존 정책이 없다** — Firestore에 무기한 쌓인다. 개인정보가 섞일 수 있으므로 보존 기간과 삭제 방법을 정한다. 1번(전역 공개)과 겹치면 영향이 커진다.

## H. 운영 · 배포

- [x] **35. ✅ `main` 브랜치 보호와 필수 체크** — `frontend`·`backend` 체크를 required로 걸었다. `enforce_admins: false`는 배포 경로를 위한 의도된 예외다.
- [ ] **36. ⚠️ CI가 빨간 채로 머지된 전례가 있다** — 2026-09-10 10:04부터 09-11까지 `main` CI가 계속 실패였는데 아무도 보지 않았고, 그동안 **다음 배포에서 채팅이 전부 500이 되는 버그**가 들어 있었다. 게이트가 실제로 동작하는지 확인한다.
- [ ] **37. ⚠️ 배포본이 `main`보다 오래되면 고친 것이 프로덕션에 없다** — 1·2·5번은 전부 백엔드 수정이라 배포되지 않으면 고친 게 아니다. **`GET /`의 `build` 값과 `main`의 SHA를 비교해 매번 확인한다.**

---

## 다음으로 할 것

**1번(대화 칸막이)과 24번(버전 고정)은 2026-09-11에 고쳤다.** 남은 것 중 순서는 이렇다.

| 순위 | 항목 | 이유 | 크기 |
|---|---|---|---|
| **1** | **16·17·18 — SRI + CSP + 클릭재킹** | 셋이 한 덩어리라 `vercel.json` 한 번이면 끝난다. CSP에 `unpkg`·`jsdelivr`·`onrender.com`을 빼면 지도와 API가 죽는다 | 1시간 |
| **2** | **25 의존성 취약점 스캔** | 24번을 고쳐 버전이 고정됐으니 이제 스캔이 의미를 갖는다 | 30분 |
| **3** | **29 git 이력 유출 점검** | gitignore는 지금부터만 막는다. 한 번은 전체를 훑어야 한다 | 30분 |
| **4** | **34 대화 보존 정책** | 1번으로 노출 범위는 줄었지만 무기한 보관은 그대로다 | 판단 필요 |

매 배포 뒤에는 **37번**을 확인한다 — 고쳐도 올라가지 않으면 고친 것이 아니다.
