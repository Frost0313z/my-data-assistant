# 버전을 고정한다 (2026-09-11).
#
# 전에는 이름만 적혀 있었다. Render가 재빌드할 때마다 **그날의 최신**을 받는 뜻이라
# 어제 되던 것이 오늘 깨질 수 있고, 업스트림이 오염되면 그대로 들어오고, 무엇보다
# 같은 커밋을 두 번 배포해도 같은 것이 올라간다는 보장이 없다.
#
# 여기 적힌 값은 pytest 146개를 통과시킨 조합이다.
#
# **직접 의존성만 고정하고 전이 의존성은 pip에 맡긴다.** 전부 고정(lock)하면 더
# 엄밀하지만, Windows에서 뽑은 목록에는 리눅스에 없는 패키지가 섞여 Render 빌드가
# 깨진다. 여기서 얻을 것(예상치 못한 메이저 업그레이드 차단)은 이걸로 충분하다.
# 더 엄밀하게 갈 거라면 리눅스에서 pip-compile로 requirements.lock을 만든다.
#
# 올릴 때: 값을 바꾸고 pytest를 돌린 뒤 커밋한다. 조용히 바뀌지 않게 하는 것이 요점이다.
fastapi==0.141.1
uvicorn[standard]==0.52.4
firebase-admin==7.5.0
openai==3.13.0
python-dotenv==1.2.3
# C7 에러 트래킹. SENTRY_DSN이 없으면 아무 일도 하지 않는다.
sentry-sdk==2.69.1
