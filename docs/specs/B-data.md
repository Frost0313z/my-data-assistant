# B. 데이터 · 분석

> **상위**: [스펙 인덱스](README.md) · [문서 허브](../PORTFOLIO_SPEC.md) · **갱신**: 2026-09-08
> 범례: **P0/P1/P2** 우선순위 · **S** ≤0.5일 / **M** 1~2일 / **L** 3일+ / **L+** 상한 근처

> **작업 전 필독**: [`../decisions.md`](../decisions.md) **결정 ③(실데이터 전량 적재)** — 이 결정이 B2·B3·C5의 성격을 바꿨다.

---

| ID | 항목 | P | 공수 | 내용 | 수용 기준 |
|---|---|---|---|---|---|
| **B1** | `insights.md` 자동 생성 스크립트 | P1 | M | 지금은 수기 큐레이션. 분석 저장소의 집계 CSV/노트북 출력에서 리포트를 재생성하는 `scripts/build_insights.py` 작성 | 스크립트 실행 → `insights.md` 재생성, 수기 버전과 수치 일치 |
| **B2** | 실데이터 Firestore 적재 (**전량**) | P1 | **L+** | 82 행정동 × 6 시점(2025-03~2026-06) × 업종 단위 실집계를 **전량** `data` 컬렉션에 적재(스키마에 `district`/`dong`/`industry` 필드 추가). *(결정 ③: 샘플 아님, 전체)* 레코드 수가 수천~1만+ 규모 → 시드 스크립트를 **배치 쓰기(`batch`, 500건 단위)** + 재실행 안전(upsert)으로 확장 | `/api/data`가 실제 상권 레코드를 페이지네이션으로 반환하고, 필터별 요약이 의미 있는 값을 낸다. **재실행해도 중복 적재 안 됨** |
| **B3** | 요약 API 필터 파라미터 | P1 | M | `/api/data/summary?district=&dong=&industry=&from=&to=` — 필터별 집계. A3b의 화면 상태와 파라미터 매핑. **결정 ③으로 필수성 상승** — 전량 데이터에선 전체 요약이 무의미하고, 필터가 있어야 대화가 성립한다 | 필터를 주면 해당 부분집합의 요약이 나오고, 화면 상태와 일치한다 |
| **B4** | 트렌드 판정 고도화 | P2 | S | "증가/유지/감소" 이분법 → 선형회귀 기울기 + 최근 구간 가중. 근거 수치 함께 반환 | 요약 `trend`가 기울기·변화율을 포함한다 |
| **B5** | `insights` RAG(임베딩 검색) | P2 | L | 리포트가 커지면 전체 주입 대신 질문 관련 청크만 검색해 주입. 임베딩 캐시 | 프롬프트 토큰이 상한 아래로 유지되고, 답변 품질이 전체 주입 대비 유지된다 |

---

## 주의

- **B2 재실행 안전성이 수용 기준이다.** 대량 적재는 중간 실패가 잦으므로 upsert가 아니면 중복이 쌓인다.
- **B2 완료 후 D3(시드 리셋)의 성격이 바뀐다** — 복구 대상이 커져 배치 삭제·재적재가 필요하다. → [`C-reliability-ops.md`](C-reliability-ops.md)
- **B2 완료 후 C5(요약 캐시)가 사실상 필수가 된다** — 매 채팅마다 전량 집계는 비현실적이다.
- 수치를 바꾸면 `frontend/js/topics-data.js`와 `backend/app/seed/insights.md`가 **같은 출처를 써야 한다.** 한쪽만 고치지 않는다.
