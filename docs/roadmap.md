# 로드맵 · 리스크 · 진행 현황

> **상위**: [문서 허브](PORTFOLIO_SPEC.md) · **갱신**: 2026-09-09
> **진행 상태는 이 파일에서만 관리한다.** 스펙 파일에 상태를 중복 기록하지 않는다.
> **§3 진행 현황은 R0(조율자) 소유**, 나머지 절은 R6 소유다. ([`roles/README.md`](roles/README.md))

---

## 1. 단계별 실행 계획

### Phase 1 — 대시보드 통합 ✅ **완료**

핵심 포트폴리오 서사를 완성한다.

- **포함**: A1 · A2 · A2a · ~~A3a~~ · A3b · **A6** · A7 · A8 · F3 · E1 · E5
- **완료 기준**: 라이브 URL에서 주제를 고르고 물으면 AI가 그 주제를 인지하고 답한다.
- **결과**: A1·A2·A2a·A3b·A7·A8·F3 완료. **E1·E5는 미착수로 남음** → Phase 1-b로 이월.

### Phase 1-b — 사용자 여정 보강 ✅ **완료** (2026-09-09)

[벤치마킹](research/benchmark-golmok.md)·[여정 분석](research/user-journey.md)에서 진단한 두 구멍(**Discovery** 진입 질문 부재, **Action** 종착점 부재)을 메운다.

- **포함**: A9(목적 칩) · A10(주제별 제안 질문) · A11(AI 인사말 온보딩) · A12(기준 배지 + 한계 고지) · A14(주제 리포트 CTA) · A13(대화 제목 주제 태그) · **A16(웹폰트 결함 수정)** · **A21(대기 상태 — 문구 정확성 결함 포함)** · **E1 · E5(Phase 1 이월)**
- **완료 기준**: 처음 방문한 사람이 **목적 칩 하나 → 답변 → 리포트**까지 이탈 없이 도달한다. 주제를 바꾸면 제안 질문도 함께 바뀐다. 콜드스타트여도 사용자가 기다릴 수 있다.
- **예상 공수**: A9 M + A14 M + E5 M + 나머지 S ×7 ≈ 6일
- **결함 2건 동반 수정**: `A16`(의도한 서체가 적용된 적 없음) · `A21`(콜드스타트 안내가 "재시도하라"인데 실제로는 자동 완료됨)
- **최소선**: **A9 + A10**만 해도 Discovery 구멍이 닫힌다. A14는 그다음 우선.
- **A16 예외**: 감성 항목이지만 *의도한 서체가 적용된 적이 없는* 결함이라 여기서 함께 고친다(S).

### Phase 2 — 디자인 시스템 ✅ / 실무 완성도 ✅ **완료** (2026-09-09)

- **디자인**: A15 · A17 · A18 · A19 · A20
- **실무**: C1(pytest 136개) · C2(CI) · C4(로깅) · C5(요약 캐시) · C6(예열) · D3(시드 리셋) · A4(스트리밍 + 마크다운)
- **완료 기준**: CI가 초록이고 테스트가 핵심 경로를 덮는다. 채팅이 스트리밍된다. 데모 데이터를 언제든 복구할 수 있다. 로그로 요청·비용을 추적할 수 있다.
- **순서 주의**: **A15 → F1(색 대비 검증)**. A15가 끝났으므로 **F1을 지금 할 수 있다.**

### Phase 1-c — 지도 중심 진입 ✅ **완료** (2026-09-09)

첫 화면의 주역을 지도로 바꾼다. 문제는 데이터가 아니라 **표현**이다 — 대시보드에 정보는 다 있는데 *"이 지역이 어떤 곳인가"* 로 읽히지 않는다.

- **포함**: A22 · A23 · A24 · A25 · A26 · A27 · A28 — **전부 완료·검증**
- **완료 기준**: 처음 온 사람이 지도에서 동을 하나 고르면 **그 동네가 어떤 곳인지** 유형·근거 수치·업종 구성으로 읽히고, 이어서 AI에게 물을 수 있다.
- **순서 주의**: **A23 → A27**. A23이 `map&metric=` 3종을 우리 지도로 흡수해 임베드 패널이 8개 → 5개로 줄고, 그래야 하단 탭이 한 줄에 들어간다.
- **근거**: [`decisions.md`](decisions.md) ⑦ · 데이터 자산은 [`specs/B-data.md`](specs/B-data.md) §인벤토리

### Phase 3 — 심화 ✅ **완료** (2026-09-09) / E3 녹화만 남음

- **포함**: B1 · B2 · B3 · B4 · C3 · C7 · E2 · E4 · F1 · F2 **완료** · E3 **대본만** · B5 **안 함(근거 기록)**
- **완료 기준**: 실제 상권 데이터가 들어가 요약이 의미를 가진다. 문서(ADR·회고·데모)가 갖춰진다. 접근성·성능 목표 달성.
- **결과**: 접근성 Lighthouse **100**, LCP **713ms**(로컬). 데모 **녹화만 남음**.

> **최소 제출 가치선**: Phase 1만으로도 포트폴리오 제출 가치가 있다. Phase 2·3은 확보 가능한 시간에 따라 취사선택.

---

## 2. 리스크와 완화

| 리스크 | 영향 | 완화 |
|---|---|---|
| 대시보드 1.8MB 단일 파일 | 초기 로드 지연 | 지연 로드 + 로딩 상태(구현됨). 필요 시 데이터/뷰 분리 검토(Phase 3) |
| Render 무료 콜드스타트 30~50초 | 데모 첫인상 저하 | C6(외부 핑) + **A21(단계적 대기 문구)**. 데모 직전 수동 워밍업. **요청 자체는 자동 완료됨**(실측 43초) — 현재 안내가 "재시도하라"인 것은 오안내라 A21에서 함께 고친다 |
| OpenAI 비용 폭주 | 요금 | C3(rate limit + 일일 토큰 상한). 출력은 `CHAT_MAX_TOKENS`(900) / `REPORT_MAX_TOKENS`(1200), 입력은 **캐시 프리픽스 + 이력 상한 8메시지**로 억제 → §3 [토큰 전략](#토큰-전략-2026-09-09) |
| 범위 팽창 | 미완성으로 마감 | Phase 1을 독립적으로 제출 가능하게 유지. P2 항목은 언제든 드롭 |
| 대시보드 HTML 수정이 분석 저장소에 영향 | 분석 결과물 훼손 | A2a는 **추가만** 하는 격리된 블록(head CSS 주입 + `change` 이벤트 디스패치). 기존 로직 미변경. 파라미터 없으면 기존과 동일 동작 |
| 배포 브랜치와 실제 배포본 불일치 | 고친 게 라이브에 없는데 원인 오진 | `openapi.json` 스키마로 확인. 실제로 브랜치 설정이 안 먹은 사례 있음 |
| 문서 분할로 인한 내용 중복·불일치 | 스펙이 두 곳에서 갈라짐 | 상태는 이 파일에서만, 구현 값은 `design/design-system.md`에서만. 나머지는 링크로 참조 |

---

## 3. 진행 현황

**최종 갱신** 2026-09-09 · **검증 브랜치**: `feat/portfolio-phase1b` (Phase 1의 `main` 머지: `0aed7ae`)

| ID | 상태 | 메모 |
|---|---|---|
| A1 레이아웃 재구성 | ✅ 구현·로컬 검증 | 3분할 그리드 + 좁은 화면 하단 탭. 데이터 관리는 접이식으로 이동 |
| A2 주제 버튼 + 패널 임베드 | ✅ **라이브 검증** | 8개 주제, 주제별 실제 대시보드 패널 임베드, 딥링크. 데스크톱 4×2 / 모바일 2열. 재클릭 해제 확인 |
| A2a 대시보드 섹션만 보기 모드 | ✅ 구현·배포 | 분석 저장소 `286eea1`·`fec91eb` push, GitHub Pages 반영. `only`/프리셋 전부 동작 확인 |
| ~~A3a~~ | 폐기 | 분석 저장소 스크립트 revert `e0ee2d4` push 완료 |
| A3b 선택 주제 → 프롬프트 주입 | ✅ **라이브 검증** | `topics.js` → `screenContext={topic}` → `context` → `[선택한 분석 주제]` 블록 |
| A7 주제 전환 피드백 | ✅ **라이브 검증** | 프롬프트 규칙 2건 + 대화 구분선. 잔존율→업종별 전환 시 "업종별 증감 기준으로 보면…"으로 정확히 전환 |
| A6 모바일 탭 전환 | ✅ 구현·로컬 검증 | `tabs.js` + `@media (max-width:900px)`. **A1 작업에 포함돼 진행됐으나 어느 Phase에도 기록되지 않았던 항목** — 문서 점검(2026-09-08)에서 발견해 반영 |
| A8 토큰 사용량 표시 | ✅ **라이브 검증** | "토큰 1,730 (입력 1,688 · 출력 42)" 답변마다 표시 |
| F3 전역 에러 상태 UI | ✅ 구현·로컬 검증 | 콜드스타트 / AI 실패 / 일반 각각 문구 + 재시도. 패널 로드 실패도 재시도 버튼 |
| A9 목적 칩 | ✅ **브라우저 검증** · A28로 대체 | `purpose-chips` 3개로 시작(`81e87e4`)했으나 A28이 페르소나 카드로 승격하며 흡수. 현재 DOM에 `.purpose-chip` 0개 — 목적 진입 경로는 A28 항목에서 관리한다 |
| A10 주제별 제안 질문 | ✅ **브라우저 검증** | 지표 5종 전환마다 3문항 교체 확인. **회귀 1건 발견·수정** — 전송 시 비운 제안을 복구하지 않아 첫 질문 뒤로 다음 질문 경로가 사라졌다 (`19a80dd`) |
| A11 첫 방문 온보딩 | ✅ **브라우저 검증** | `localStorage` 비운 상태에서 인사말 버블 표시, 한계 고지 문장 포함 |
| A12 기준 배지 | ⛔ **제거됨** (2026-09-09, 사용자 요청) | 앰버 배지 2줄을 걷어내고 그만큼 지도에 줬다 — 실측 데스크톱 336→417px, 모바일 538→618px. **한계 고지는 사라지지 않았다** — 지도 아래 주석(`공급 밀도가 높다고 매출이나 수익성이 높은 것은 아닙니다`), 첫 인사말, 지표별 해설, 리포트 [한계] 블록에 남아 있다. 다만 **항상 보이던 자리는 없어졌다** — A12가 노린 것이 그 자리였다. **같은 날 지표별 해설도 제거됐다(아래 A23) — 상시 노출되는 한계 고지는 이제 주석 한 줄뿐이다** |
| A23 지표 해설 줄 | ⛔ **제거됨** (2026-09-09, 사용자 요청) | 지표별 해설 1줄 + 2D/3D 안내 1줄(각 33px)과 지도 둘레 여백 32px을 걷어냈다 — 합 98px을 전부 무대가 가져갔다. 실측 **데스크톱 484→582px, 모바일 513→611px**(같은 창 A/B). `METRICS[].note` 7개와 `withObjectParticle()`이 죽어서 함께 지웠고, 모바일 고정식은 `100dvh - 320px` → `- 222px`로 맞췄다. 램프·범례·단위 전환(A23 본체)은 그대로다 |
| A13 대화 제목 주제 태그 | ✅ 실제 API 검증 | `[전체 규모·성장]` 제목 태그와 대화 저장·불러오기 확인 (`edc8e1c`) |
| A14 주제 리포트 | ✅ 실제 API 검증 | 화면 CTA → `context.mode=report` → 요약·핵심 수치·해석·한계 4블록, 매출·유동인구 한계 포함 확인 |
| A16 웹폰트 로드 | ✅ **브라우저 검증** | `getComputedStyle(body).fontFamily`가 `Pretendard`로 해석됨 (`f1f5dff`) |
| A21 대기 타이머 | ✅ **브라우저 검증** | `api.sendChat`만 지연 스텁으로 갈아 끼워 17초 대기를 만들어 3단 전부 관측(비용·쓰레기 레코드 0). 1초 `생각하는 중...` / 6초 `(6초)` 병기 / 16초 `절전에서 깨어나는 중… 요청은 계속 진행 중입니다. (16초)` / 응답 도착 시 타이머 정지 (`1389b48`) |
| F3 문구 정확성 | ✅ **브라우저 검증** | 백엔드를 실제로 내리고 확인했다. `cold`가 아니라 `connection`으로 분류되어 "서버에 연결하지 못했습니다… 다시 시도할 때 깨어납니다" + 재시도 버튼이 뜨고, 백엔드를 올린 뒤 그 버튼으로 복구됐다. 요약 패널도 별도 실패 문구로 처리 (`3da4780`) |
| A15 디자인 토큰 | ✅ **브라우저 검증** | 코덱스(R2). 실행 중인 CSSOM에서 **색상 리터럴 `:root` 밖 0건 / `:root` 안 33개**. 타입 위계 복구 (`fd65f20`) |
| A17 채팅 온기 분리 | ✅ **브라우저 검증** | AI 버블 실측 `rgb(251,247,241)` = `--warm-050 #FBF7F1`, 사용자 버블은 `--primary` (`88caf57`) |
| A18 모션 도입 | ✅ **브라우저 검증** | CSSOM 전수 조사에서 지속시간이 120·150·200ms **3종뿐**(그 외는 reduced-motion의 0.01ms). `prefers-reduced-motion` 규칙 존재 확인 (`1d8dde5`) |
| A19 증감 중립 색 | ✅ **브라우저 검증** | 5개 탭 헤드라인에서 `trend-up:+2,800` `trend-up:+4.39%` `trend-down:−2.1%` 등 마크업 적용 확인. 쉼표·부호 삼킴 없음 (`1df5b48`·`2febf79`) |
| A20 서비스 아이덴티티 | ✅ 완료 | 사용자 확정명 **대전 상권분석 매니저**를 제목·README·공유 메타에 반영. [`decisions.md`](decisions.md) ⑥ |
| A22 지도 중심 진입 | ✅ **브라우저 검증** (2026-09-09, 사용자) | 코덱스(사용자 직접 지시). MapLibre GL 5.6 + 82개 행정동 2D/3D(`fill-extrusion`), MapLibre 부재 시 인라인 SVG 폴백(3D 비활성). 자치구/행정동/시점 선택 · 지도 클릭 → `screenContext`. 레이아웃 3분할 → 2분할, 사이드바·주제 영역 접힘. 채팅 접기는 조율자 추가. 경계 데이터는 `vuski/admdongkor`(MIT) + 구 경계 OSM(ODbL), `AttributionControl`로 표기. **결정 ⑦로 뒤늦게 기록** |
| A23 지도 지표 전환 | ✅ **브라우저 검증** | 지표 5종 전환마다 범례·해설·`screenContext` 동시 교체, 교체율 선택 시 시점 선택 실제로 `disabled`. 지표 5종(밀도·업소수·잔존율·교체율·HHI). 밀도만 고정 구간(대시보드와 동일), 나머지는 5분위 → **82개 동이 16/16/17/16/17로 흩어짐 확인**. 교체율은 전 기간 누적이라 시점 선택 차단. 값 옆에 업소 수 병기(밀도 왜곡 보정). 파일 365→388KB (`2c66de4`) |
| A24 행정동 라벨 | ✅ **브라우저 검증** | 대전 전체 축척에서 마커 87개 렌더. `symbol` 대신 **Marker** — 스타일에 `glyphs`가 없어 `text-field`를 못 쓴다. 충돌 회피를 직접 구현(폴리곤 46px 이상일 때만 표시) → 시내 축척 **22~27개/82개** 실측. 선택 동은 항상 표시 (`7d26737`) |
| A25 업종별 점 레이어 | ✅ **브라우저 검증** | 업종 셀렉트 11항목(`전체 (80,704개)` + 10종), 고정 시점 고지 노출. 2,321점 × 10업종, 합계 **80,704 = 전체 업소 수 일치**. 배열 인코딩으로 **54KB**(추정 203KB의 1/4)라 지연 로드 없이 같이 받고 레이어만 숨김. 최신 1시점 고정 — 시점 바꿔도 안 움직인다고 화면에 고지 (`463a776`) |
| A28 페르소나 진입 | ✅ **브라우저 검증** | 카드 3개 → 선택 시 접힘·`persona_v1` 저장·지도 지표 전환·질문 자동 전송·`바꾸기` 복귀까지 전 경로 확인. 칩 → 카드 승격 + `localStorage` 지속. 지도 지표·제안 질문·**AI 프롬프트**까지 연동. 건너뛰기·되돌리기 경로 확보 (`cffda06`·`3a14ae5`·`51b8190`) |
| A26 지역 유형 보기 | ✅ **브라우저 검증** | 지표 셀렉트 6번째 항목. 밀도 중앙값 × 교체율 중앙값 2축 → **82개가 21/17/17/21로 갈리고 6개는 유형에 넣지 않았다**(중앙동=밀도가 75분위의 3배 초과 · 나머지 5개=업소 200개 미만). 선택 시 유형·뜻·판정에 쓴 두 값과 기준선을 함께 표시하고, 뺀 동은 뺀 이유를 적는다. 색은 색상(교체 축)×명도(밀도 축) 범주형 팔레트, 3D 높이 미사용. 기준선은 시점마다 다시 잡는다(2025-03 밀도 48.6 → 2026-06 50.2 실측). **업종 구성은 데이터가 없어 뺐다** — 점 데이터가 행정동 단위로 안 맞는다([`specs/B-data.md`](specs/B-data.md) §점 데이터의 한계). **검증 중 결함 1건**: 유형은 화면이 만든 구분이라 AI가 뜻을 몰라 *"좋은 지역 유형이라고 할 수 있습니다"* 라고 단정하고 근거로 LQ 3.48을 끌어왔다 — 수용 기준 정면 위반. `context.regionType`(값 화이트리스트 5종) + 백엔드 `REGION_TYPE_HINTS`로 고쳤고, 같은 질문에 *"평가하기는 어렵습니다… 매출·유동인구가 필요합니다"* 로 바뀐 것을 확인했다 |
| A27 하단 무대 전환 탭 | ✅ **브라우저 검증** · 결함 2건 수정 | 무대 전환·`aria-pressed` 배타 확인. **모바일에서 탭이 하단 탭바에 27px 가려졌고, 넓은 화면에서는 범례·선택 결과·질문 버튼이 접힘선 아래로 밀려 있었다** — 둘 다 `19a80dd`에서 수정. 지도와 분석 패널이 같은 무대 공유 → 세로 예산 경쟁 해소. 접이식 제거하며 목적 칩(A9)을 밖으로 꺼냄. 지도 변형 3종 제거로 주제 8→5개, 해설은 지표 주석으로 이관 (`8b3026c`·`02c0a0e`) |
| A4 채팅 스트리밍 + 마크다운 | ✅ **브라우저 검증** | SSE `POST /api/chat/stream`. 실측 **498조각** 수신. 마크다운은 화이트리스트 렌더러 — `innerHTML`을 한 번도 쓰지 않아 스크립트 실행 경로 자체가 없다. 공격 문자열 8종 + 원문 검사 (`28e479d`) |
| A4a 답변 품질 | 🟡 부분 해소 | 서식 문제는 A4로 해결. 문단 길이·수치 인용 밀도·반복은 **사용자 판단으로 보류** |
| B1 리포트 수치 대조 | ✅ 실행 검증 | 통째 재생성이 아니라 **대조**로 성격 정정. 인용 9건을 데이터에서 찾아 시점까지 확인. CI 포함. **문서 결함 2건을 잡았다** (`72dfaca`) |
| B2 데이터 구조화 | ✅ **실데이터 검증** | "전량 적재"는 이미 돼 있었다(492 = 82×6). 없던 건 **구조** — `memo`에 뭉쳐 있던 자치구·행정동·업종을 필드로 분해. 지도 데이터와 **불일치 0건**. 리셋으로 실제 Firestore 반영, 날짜·값·메모 동일 확인 (`67d3b9b`) |
| B3 요약 필터 | ✅ **실데이터 검증** | `?district=&dong=&industry=&date_from=&date_to=` + `/api/data/dimensions`. **서구 144건 +4.39%** — 리포트의 서구 성장률과 일치 |
| B4 트렌드 판정 | ✅ **실데이터 검증** | 앞뒤 절반 비교 → 시점별 합계 최소제곱 기울기. `slope_per_period`·`change_pct` 동반. 실데이터 **상승 / +3.59%** (`27fa84e`) |
| B5 insights RAG | ⛔ **안 함(근거 기록)** | 지금 하면 손해. 프리픽스 2,460자 → 650자로 캐시 임계 미달, 순이득 약 180토큰. **리포트 4,000자 초과 시 재검토** |
| C1 pytest 스위트 | ✅ 실행 검증 | **136개 통과**, 외부 호출 0. `append_turn` 경합을 트랜잭션으로 교체 — **옛 코드가 실제로 턴을 잃는 것까지 확인** (`42ff12e`) |
| C2 GitHub Actions CI | ✅ **푸시·실행 확인** (2026-09-10, `7c0bf6d`에서 frontend 9s · backend 19s 통과) | 백엔드 pytest(자격증명 없이) + 프론트 문법·빌드·마크다운·지도 데이터·리포트 대조. **배포 차단은 못 한다** — 브랜치 보호는 대시보드 작업 (`2dcc598`) |
| C3 남용 방어 | ✅ 실행 검증 | 분당 10회(XFF 기준) + 일일 30만 토큰 → 429 + `Retry-After`. 프론트는 429를 고장이 아니라 정책으로 안내 (`086a13e`) |
| C4 구조적 로깅 | ✅ 실행 검증 | JSON 로그 + `X-Request-Id` 왕복 + `cached_tokens`·`truncated`·비용. `GET /`에 `build` 추가 — **배포본 확인 수단이 생겼다** (`19b906b`) |
| C5 요약 캐시 | ✅ 실행 검증 | TTL 30초 + 쓰기 시 즉시 무효화. **채팅 3회 → 읽기 1회** (`4849230`) |
| C6 콜드스타트 예열 | 🟡 구현 완료 · **미푸시** | GitHub Actions 10분 `GET /`만(계정 불필요). 실측 **42.7초** 재확인. 예약 실행은 best-effort고 60일 무커밋이면 꺼진다 (`4d6a1bc`) |
| C7 에러 트래킹 | ✅ 실행 검증 | DSN 없으면 안 켜지고 SDK 없어도 앱이 뜬다. 질문·답변은 `before_send`로 제거. **프론트는 미적용** |
| D3 시드 리셋 | ✅ **실데이터 검증** | `POST /api/dev/reset`. 토큰 없으면 **404로 감춤**. 실제로 492건 복구 + 대화 0 + 캐시 무효화 확인 |
| E2 ADR | ✅ 구현 완료 | 5건. 각각 맥락·**대신 잃은 것**·언제 뒤집나 (`6a690bd`) |
| E3 데모 | 🟡 **대본만** | 60초 대본 + 녹화 주의. **화면 녹화는 사람이 해야 한다** |
| E4 회고 | ✅ 구현 완료 | `RETROSPECTIVE.md` |
| F1 접근성 | ✅ **Lighthouse 100** | 색 대비 위반 0건(전수 계산). 스킵 링크·`aria-live`·스트리밍 중 `aria-busy` (`9b77cd9`) |
| F2 성능 | 🟡 **로컬만** | LCP **713ms** / CLS **0.00**. 캐시 헤더 추가. **배포본 재측정 남음** |
| E1 아키텍처 구성도 | ✅ 구현 완료 | 배포 토폴로지 mermaid 추가 (`9a13ae8`) |
| E5 README 포트폴리오용 재구성 | ✅ 구현 완료 | 미션 항목은 부록 A~G로 보존 (`9a13ae8`) |

> **🟡 = 해당 항목의 검증이 아직 완료되지 않음.** 2026-09-09 원본 작업 폴더의 가상환경과 기존 설정을 재사용해 현재 브랜치의 백엔드를 실행했다. 브라우저에서 492건 요약, 일반 AI 답변, 리포트 4블록, 대화 저장·불러오기를 확인했고 점검용 대화만 삭제했다. 배포된 API도 동일한 492건 요약을 반환했다. 데이터 재적재·API 주소 변경·배포는 하지 않았다.
> **검증 전에는 완료로 올리지 않는다.**

### 브라우저 검증 (2026-09-09, R0)

`localhost:5500`(정적) + `localhost:8000`(백엔드)로 Chrome DevTools에서 실측했다.
**콘솔 에러 0건.** 기준 상태는 **conversations 0 / data 492**로 복구했다 —
점검 중 실제로 만들어진 대화 3건은 API로 삭제했다.

측정은 스크린샷 눈대중이 아니라 `getBoundingClientRect`·CSSOM 전수 조회로 했다.
"보인다"와 "접힘선 안에 있다"는 다른 문제라서, 셋(3)번 결함은 스크린샷만 봤으면
못 잡았다 — 화면에는 탭이 멀쩡히 보였고 그 아래가 가려져 있었다.

**찾은 결함 4건은 전부 수정**(`19a80dd`) — 위 A10·A27 행 참조. 조사 오류
(`업종 집중도을(를)`)도 지표 5종 전부 재확인했다.

**남은 것**

| | 처리 |
|---|---|
| A4 마크다운 렌더 | **향후 개선으로 보류**(사용자 판단). 답변의 `### 종합 해석`이 서식 없이 노출되는 건 A4가 들어가면 해소된다. [`specs/A-product-ux.md`](specs/A-product-ux.md) A4a |
| `CHAT_MAX_TOKENS=500` | **해결** — 900으로 올리고 입력 쪽을 대신 줄였다. 아래 [토큰 전략](#토큰-전략-2026-09-09) |
| F3 `connection` 재분류 | **해결** — 백엔드를 내리고 확인 후 되올렸다. 위 F3 행 |

### 토큰 전략 (2026-09-09)

답변이 정확히 500토큰에서 잘렸다. 상한을 올리면 비용이 오르니, **상한은 올리고
입력을 줄이는 쪽**으로 맞바꿨다.

| | 전 | 후 |
|---|---|---|
| `CHAT_MAX_TOKENS` | 500 (잘림) | **900** — 실측 재답변 470토큰으로 완결. 상한이 답변을 늘리지 않고 잘림만 없앴다 |
| 캐시 고정 프리픽스 | 약 60자 (임계 미달 → 항상 0) | **2,432자 = 전체의 95%.** 실측 `cached_tokens=1024` 확인 |
| 대화 이력 | 무제한 | 최근 **8메시지**. 입력이 대화 길이에 비례해 늘던 유일한 경로였다 |

**리포트 본문(1,783자)을 주제별로 잘라 주입하는 안은 채택하지 않았다.** 사용자는
화면에 없는 주제도 자유롭게 묻고, 그때 "리포트에 없다"고 답하면 절약이 아니라
품질 저하다. 근거는 [`decisions.md`](decisions.md) §설계 판단.

프리픽스가 깨지면 응답은 멀쩡한데 비용만 조용히 두 배가 되므로 `tests/test_chat.py`에
프리픽스 길이 검사를 남겼다. OpenAI 자동 캐싱은 best-effort라 매 요청 적중하지는
않는다 — 2회 연속 호출에서 1024 / 0으로 갈렸다.

### Phase 1 종료 기록 (2026-09-08)

- `feat/portfolio-phase1` → `main` `--no-ff` 머지(`0aed7ae`), push, Render 배포 완료.
- 라이브 검증 통과: 배포 스키마에 `context`·`usage` 존재 / 주제 전환 시 답변 정확히 전환 / 토큰 표시 / 회귀 없음(Inf·NaN·범위·날짜·빈메시지 → 422, 요약 200 · 492개).
- 테스트 대화 정리(0건), `data` 492개 유지.

### Wave 1 조율 (2026-09-09, R0)

**브랜치**: Phase 1-b 코드 작업은 `main`에서 딴 **`feat/portfolio-phase1b`** 에서 진행한다. 파일 소유권이 겹치지 않으므로 역할별로 나누지 않고 한 브랜치를 공유한다. 검증 후 `--no-ff` 머지(Phase 1과 동일 방식).

**실행자는 2개다** — 코덱스(R2) · Claude(R1·R3·R5·R6). [`roles/README.md`](roles/README.md) 최소 구성의 **2역할** 구성이다. 그래서 진짜 동시성은 두 갈래뿐이고, 나머지는 전부 Claude 안의 순서 제약이다.

```
코덱스  A16 ──▶ A12 ─────────────────────────────────────┐
                                                     H3 │
Claude  R3:A14 프롬프트 ─▶ R5:F3 문구 ─▶ R1:A10 ─▶ A11    │
                                          ─▶ A21 ─▶ A14 UI ─▶ A13 ─▶ A9 ◀┘
        R6:E1 ─▶ E5   (독립 · 어디든 끼워 넣는다)
```

**`index.html`이 필요한 R1 작업은 A9 하나뿐이다.** `#chat-messages`·`#chat-suggestions`가 이미 있어(`index.html:62-63`) A10·A11·A21·A14 UI·A13은 전부 `chat.js`·`history.js`만 건드린다. 그래서 **코덱스를 기다리는 동안 Claude가 멈추지 않는다** — A9만 마지막으로 미룬다.

**인계 — 진짜 교차 도구 인계는 H3 하나뿐이다.**

| | 주는 쪽 → 받는 쪽 | 내용 | 성격 |
|---|---|---|---|
| **H3** | R2 → R1 | A12 반영된 `index.html` 최종 형태 | **교차 도구.** 통보 필요 |
| H1 | R3 → R1 | A14 요청 방식(별도 엔드포인트 vs `context` 플래그) + 4블록 프롬프트 | 같은 에이전트 — 순서만 지키면 됨 |
| H2 | R5 → R1 | A21 대기 문구 3단 확정 문자열 + `cold` 오분류 처리 | 같은 에이전트 — 순서만 지키면 됨 |

`frontend/js/chat.js`를 R5(F3)와 R1(A10·A11·A21)이 함께 만지는 문제는 **둘 다 Claude라 자동으로 해소된다.** 다만 문구를 두 번 고치지 않도록 F3을 A21보다 먼저 끝낸다.

**A9가 최소선인데 유일하게 코덱스에 물려 있다.** A16·A12 둘 다 S(≤0.5일)라 대기는 짧아야 정상이다. 코덱스가 길어지면 R2를 Claude가 흡수하는 3역할 구성으로 내린다.

**Wave 1에 사용자 결정 대기 항목은 없다.** [`decisions.md`](decisions.md) ⑥(서비스 이름)은 A20만 막고 A20은 Wave 2다.

**기준 상태 재확인 (읽기 전용)**: R2 브리핑의 실측값 7건이 코드와 전부 일치 — 브리핑이 낡지 않았다. `docs/` 깨진 링크 0건. `backend/tests/`·`.github/`는 아직 없음(각각 C1·C2에서 생성, Wave 1 영향 없음).

### Wave 1 종료 기록 (2026-09-09)

**Wave 1 스펙 11건 전부 구현 완료.** 브랜치 `feat/portfolio-phase1b` (미푸시), 커밋 10건.

코덱스(R2)는 Orca 오케스트레이션으로 디스패치했다 — `run_5d1b5a688120` /
`task_39f6adf8e55d` / `ctx_4db27176a7a9`. `worker_done` 수신 후 수용 기준을
**보고에 의존하지 않고 직접 확인**했다: `.topics-foot` 0건 · `.purpose-chips`
미생성 · Pretendard 실제 로드 · `[hidden]` 리셋 보존 · `height:100vh` 개수
변화 없음(회귀 아님) · `--caution` 값이 문서와 일치.

**조율 중 발견한 것**

| | 내용 |
|---|---|
| **A13 배정 오류** | 역할 문서는 A13을 R1(`frontend/js`)에 뒀으나 제목은 `conversation_service.append_turn`이 서버에서 만든다. 프론트에는 제목을 넘길 경로 자체가 없어 **백엔드 변경이 불가피**했다 |
| **README 3건 낡음** | 프롬프트를 "두 블록"으로 기술(실제 3블록) · 화면을 "4개 영역"으로 기술(실제 3분할+주제 버튼) · 브레이크포인트 800/560px로 기술(실제 1180/900/480px). E5에서 정정 |
| **R2 워크트리 미사용** | 사용자가 준비한 `R2` 워크트리(`Frost0313z/R2`)가 있었는데 조율자가 `terminal list`를 확인하지 않고 현재 워크트리에 코덱스를 새로 띄웠다. A12까지는 그대로 끝내고 **Wave 2부터 R2 워크트리로 이전**하기로 결정 |
| **R2에 넘길 것** | `.report-cta` · `.purpose-chips` · `.purpose-chips-label` · `.purpose-chip` 스타일이 없어 기본 button으로 렌더된다 |

### 코드 리뷰 (2026-09-09, 코덱스 R2)

코덱스가 전체 정적 검토를 돌려 `worker_done`으로 보냈다. Orca는 Wave 1 디스패치
(`ctx_4db27176a7a9`)가 이미 만료돼 **거부**했지만 본문은 남았다. 보고를 그대로 믿지 않고
**6건을 코드에서 직접 확인**했다.

| 지적 | 판정 | 처리 |
|---|---|---|
| 없는 id로 PUT/DELETE해도 성공 응답 (`data.py`·`data_service.py`) | ✅ **사실, 게다가 더 나쁨** — Firestore `set()`은 문서를 **만든다**. 오타 난 id로 보낸 PUT이 그 id를 가진 레코드를 새로 만들고 200을 돌려줬다. 이 저장소에 쓰레기 레코드가 쌓였던 전례가 있는 바로 그 종류다 | **수정** — 존재 확인 후 404. 실측 404/404/200/200, 유령 레코드 0건 |
| 채팅 동시 요청 시 대화 턴 유실 (`append_turn` read-modify-write) | ✅ 사실. 게다가 프론트에 전송 가드가 **아예 없어** 엔터 두 번이면 닿았다 | **부분 수정** — 전송 중 잠금(3회 시도 → API 호출 1회 실측). 서버 트랜잭션은 미적용 (아래) |
| 매 요청 Firestore 전체 컬렉션 읽기 (`get_summary`) | ✅ 사실 | **이미 C5**(요약 캐시). B2 전량 적재 뒤 필수 |
| CORS 기본 와일드카드 | ✅ 사실 — `ALLOWED_ORIGINS` 기본값이 `*`. 배포는 env로 좁히지만 기본값이 열려 있다 | **미수정** — 기본값을 바꾸면 env가 빠진 배포가 조용히 죽는다. C3과 함께 판단 |
| MapLibre 오류 시 2D 전환 부재 | ⚠ **절반만 사실** — 생성자 예외와 `webglcontextlost`에는 `useFallback()`이 붙어 있다. `map.on("error")` 경로에만 없다 | **미수정** — 이 지도는 외부 소스가 없어 `error`가 대개 일시적이다. 아무 오류에나 SVG로 내려가면 더 나쁠 수 있다 |
| `backend/test_models.py`가 `firebase_admin` 없어 중단 (C1에서 `tests/`로 이관) | ❌ **아님** — 환경 문제다. 프로젝트 venv로 돌리면 전량 통과한다. 코덱스가 다른 인터프리터로 실행했다 | 없음 |

**남은 것**: `append_turn`을 Firestore 트랜잭션으로 바꾸는 것. 프론트 잠금은 현실적인
트리거를 없앴을 뿐 서버는 여전히 경합에 열려 있다 — 탭 두 개면 재현된다. C1(pytest)에서
회귀 테스트와 함께 잡는 것이 맞다.

### 배포 확인 (2026-09-09)

푸시 후 실제 배포본을 확인했다. **둘 다 문제가 있었고 프론트만 고쳤다.**

#### 프론트 (Vercel) — 고침

`main` 브랜치는 제대로 보고 있었다(빌드 로그: `Branch: main, Commit: 63d38f5`).
**깨진 건 Root Directory였다** — 저장소 루트에서 `npm run build`를 돌려
`/vercel/path0/package.json` 없음으로 즉시 실패했다(`package.json`은 `frontend/`에 있다).
**21시간 동안 모든 프로덕션 배포가 `Error`였고**, 라이브에는 하루 전 Phase 1 화면이
그대로 떠 있었다.

그 위에 두 가지가 더 있었다.

| | |
|---|---|
| `vercel.json`의 `"//"` 주석 키 | **배포가 `Invalid vercel.json`으로 거부된다.** F2에서 내가 넣은 것 — vercel.json은 주석을 못 쓴다 |
| Vercel 환경변수 **0개** | `API_BASE_URL`이 없어 배포본이 `http://localhost:8000`을 호출했다. **방문자에겐 아무것도 동작하지 않는 상태** |

셋 다 고치고 CLI로 프로덕션 배포했다. 지금은 `map.js`·`markdown.js`·지도 데이터 200,
`API_BASE_URL`이 Render 주소로 치환됨, 캐시 헤더(F2) 적용 확인.

**Root Directory는 여전히 대시보드에서 `frontend`로 바꿔야 한다.** 안 바꾸면 다음
git 푸시에서 또 같은 이유로 실패한다.

#### 백엔드 (Render) — 아직 옛 코드

푸시 25분 뒤에도 배포가 안 됐다. 근거 셋 —

| 확인 | 배포본 | main |
|---|---|---|
| `GET /` | `{"status":"ok"}` | `build` 필드 포함 |
| 요약 `trend` | `유지` | `상승` (B4 회귀 판정) |
| `metrics` 키 | 4개 | 6개 (`slope_per_period`·`change_pct`) |

**같은 Firestore를 읽는데 결과가 다르다** — 데이터가 아니라 코드가 옛것이라는 뜻이다.
CORS는 정상이고(Vercel 오리진 허용), 요약도 492건을 돌려준다.

브랜치 설정·자동 배포 여부·빌드 실패 중 무엇인지는 **Render 대시보드를 봐야 안다.**

### 다음 액션

1. ~~**검증**~~ — 완료(2026-09-09). 위 [브라우저 검증](#브라우저-검증-2026-09-09-r0) 참조
2. **R2 워크트리 최신화** — `Frost0313z/R2`에 `feat/portfolio-phase1b`를 반영. **안 하면 Wave 2에서 코덱스가 `665751f` 시점의 낡은 브리핑을 읽는다**
3. Wave 2 착수 — R2(코덱스) `A15` → `A17` → `A18` → `A19`(색) · R3 `A4` → `C4` → `C5` · R5 `C1` → `C2` → `C6`
4. 완료: [`decisions.md`](decisions.md) **⑥ 서비스 이름**을 **대전 상권분석 매니저**로 확정하고 A20 반영
5. Render `Branch` 설정이 `feat/portfolio-phase1`로 남아 있으면 `main`으로 원복 (선택)
