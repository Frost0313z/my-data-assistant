# 문서 변경 이력

> **상위**: [문서 허브](PORTFOLIO_SPEC.md)
> 문서를 고치면 여기 한 줄 남긴다. (코드 변경 이력은 상위 코디세이 저장소의 `WORK_LOG.md`)

---

| 버전 | 날짜 | 내용 |
|---|---|---|
| **1.6** | 2026-09-09 | **A20 완료:** 사용자 지정명 `대전 상권분석 매니저`를 화면 제목·공유 메타·README·첫 인사말·API 제목에 반영하고 결정 ⑥을 확정했다. 원본 작업 폴더의 가상환경과 기존 연결 설정을 재사용해 현재 브랜치를 실행하고, 브라우저에서 492건 요약·AI 답변·리포트 4블록·대화 저장 및 불러오기를 확인했다. 점검용 대화는 삭제했으며 기존 데이터와 API 연결 로직은 변경하지 않았다. |
| **1.5** | 2026-09-09 | **Wave 1(Phase 1-b) 구현 완료 — 스펙 11건.** A9·A10·A11·A12·A13·A14·A16·A21·F3·E1·E5. R2(A16·A12)는 코덱스에 Orca 오케스트레이션으로 디스패치하고 `worker_done` 수용 기준을 조율자가 직접 재확인했다. **E5에서 README와 실제의 불일치 3건 정정** — 시스템 프롬프트를 "두 블록"으로 기술했으나 A3b 이후 실제로는 `[선택한 분석 주제]`가 더해진 3블록이고, 화면을 "한 페이지에 4개 영역"으로 기술했으나 A1의 3분할 레이아웃과 주제 버튼이 반영돼 있지 않았으며, 반응형 브레이크포인트를 800/560px로 적었으나 실제 CSS는 1180/900/480px에 900px는 세로 쌓기가 아니라 탭 전환이다. **E1** 배포 토폴로지 구성도 신설 — GitHub Pages(별도 저장소) 대시보드를 iframe으로 끌어온다는 사실이 기존 시퀀스 다이어그램에 없었는데, 그게 "시각화를 이중 관리하지 않는다"는 판단의 근거다. **배정 오류 1건 발견** — 역할 문서가 A13을 R1(`frontend/js`)에 뒀으나 대화 제목은 `conversation_service.append_turn`이 서버에서 만들어 프론트만으로는 구현할 수 없다. **A14 요청 경로 결정** — 별도 엔드포인트가 아니라 `context.mode="report"`. 리포트가 채팅 턴이라 대화 이력·제목·토큰 표시를 재사용하며, `mode`는 키뿐 아니라 값도 화이트리스트로 검증한다. 리포트가 4블록을 채우려면 상한이 달라야 해 `REPORT_MAX_TOKENS`를 분리했다(`CHAT_MAX_TOKENS=500`이면 마지막 `[한계]`가 잘린다). |
| **1.4** | 2026-09-09 | **Wave 1(Phase 1-b) 조율 — R0 점검에서 결함 3건 수정.** ① **A9와 A12가 같은 DOM 지점을 노렸다** — A12는 "주제 버튼 바로 위 배지", A9는 "주제 버튼 위에 목적 칩"인데 둘 다 `#topic-list` 직전이 목표이고 순서를 정한 문서가 없었다. **A12 배지가 위, A9 칩이 아래**로 확정(배지는 화면 전체의 전제, 칩은 행동 유도라 버튼에 인접해야 함). ② **A12가 성립하려면 R2가 R1 영역을 지워야 한다** — 승격 원본인 `<p class="topics-foot">`를 남기면 같은 내용이 두 번 나오므로, **R2의 `<body>` 편집 예외를 "A12 배지 신설 + `.topics-foot` 제거" 둘로 명문화.** ③ **R0 역할 정의가 세 문서에서 어긋났다** — `roles/README.md` 표는 "소유 파일 없음(읽기 전용)", `R0-coordinator.md`는 "`roadmap.md` 갱신"이 하는 일, 게다가 `docs/**`는 R6 소유였다. **`roadmap.md` §3 진행 현황 표만 R0 소유**로 확정하고 R6 브리핑에도 예외를 반영. 더불어 Wave 1 착수 순서(T0~T5)·**인계 3건(H1 R3→R1 / H2 R5→R1 / H3 R2→R1)**·브랜치(`feat/portfolio-phase1b`)를 `roadmap.md`에 기록. **R2 브리핑의 실측값 7건은 코드와 전부 일치**해 브리핑이 낡지 않았음을 확인. **Wave 1에는 사용자 결정 대기 항목이 없다**(⑥은 Wave 2의 A20만 막음). **실행자 2개(코덱스=R2 · Claude=나머지) 기준으로 착수 순서를 재작성** — 최초 표는 T0에 4개 역할이 동시 착수하는 것처럼 적었으나 R3·R5·R6이 모두 Claude라 실제로는 순차다. 진짜 동시성은 코덱스↔Claude 한 갈래뿐이고, **교차 도구 인계도 H3(R2→R1) 하나뿐**(H1·H2는 같은 에이전트 내 순서 제약으로 격하). 더불어 **R1 작업 중 `index.html`이 필요한 것은 A9 하나뿐**임을 확인(`#chat-messages`·`#chat-suggestions`가 이미 존재) — A10·A11·A21·A14 UI·A13은 `chat.js`·`history.js`만 건드리므로 코덱스 대기 중에도 Claude가 멈추지 않는다. |
| **1.3** | 2026-09-08 | **`agent-roles.md` → `roles/` 허브 구조로 전환.** 역할별 **자기완결형 브리핑 7종**(R0~R6) 신설 — 배정받은 에이전트가 **자기 파일 하나만 읽고 시작**할 수 있도록 담당 스펙·소유 파일·건드리지 말 것·현재 상태 실측값·확인 방법·완료 보고 형식을 각 파일에 담음. **R2(디자인 시스템)를 코덱스에 배정.** 다른 도구의 에이전트는 대화 맥락 없이 차갑게 시작하므로 세 가지를 반영 — ① R2 브리핑을 자기완결형으로(실측값·목표값 모두 포함) ② 수용 기준을 `grep`으로 확인 가능한 형태로 ③ **교차 도구 인계 최소화**. 이를 위해 경계 3건 조정: **A12를 R2 단독**(정적 요소라 JS 불필요, 인계 제거) · **A16을 R2 최우선**(`<head>`만 건드리므로 먼저 끝내면 `index.html` 경합 소멸) · **A19를 R2 클래스 정의 → R1 마크업 적용**으로 분담 명시. 최소 구성에 "2역할(코덱스=디자인 + Claude=나머지)" 추가. |
| **1.2** | 2026-09-08 | **에이전트 역할 분해 신설** — 남은 스펙 26건을 6개 역할(R1 UX구현 / R2 디자인시스템 / R3 백엔드 / R4 데이터 / R5 품질·인프라 / R6 문서) + 읽기 전용 조율자로 분해. 나누는 기준을 기술 스택이 아니라 **파일 소유권**으로 잡음. 충돌 지점 4곳(`index.html` head↔body / A12 마크업↔톤 / A14 프롬프트↔UI / A21 타이머↔문구)과 순서 제약 5건, Wave 1~3 병렬 계획, 규모별 최소 구성 정의. **전체 문서 점검에서 결함 2건 수정** — ① **A6(모바일 탭)이 어느 Phase에도 없고 진행 현황에도 없었다**(실제로는 A1 작업에 포함돼 구현 완료). ② **규칙 1("Primary는 한 화면에 하나")이 사용자 발화 버블과 모순**이었다 → 액션/선택상태/정체표시/링크 네 갈래로 분해하고, 누를 수 없는 정체 표시는 개수 제한에서 제외. |
| **1.1** | 2026-09-08 | **컴포넌트 스펙(`design/components.md`) · 마이크로 인터랙션(`design/motion.md`) 신설.** 도메인 핵심 컴포넌트 6종 정의 — 주제 선택기 · 대시보드 뷰포트 · 수치 헤드라인 · 기준 배지 · 주제 인식 채팅 · 주제 리포트. **모션 실측: `transition`·`animation`이 하나도 없음** — A18을 "최소화"가 아니라 "필요한 곳에만 도입"으로 재정의하고, 스케일 3단(`feedback 120` / `fade 150` / `pane 200`)만 정의. 일반 권장값(등장 400~600ms + stagger)은 3분할 화면에서 소음이라 채택하지 않음. **Delight(confetti·바운스) 전면 배제** — 매출을 모르는 데이터로 축하 연출을 하면 보증할 수 없는 것을 보증하는 셈이라 리포트 완료에도 넣지 않음. **A21 신규**(대기 상태 설계) — 콜드스타트 43초 동안 `"생각하는 중..."` 정적 텍스트뿐인 문제 + **`cold` 안내가 "다시 시도해 주세요"인데 실제로는 자동 완료되므로 오안내**인 문제를 함께 수정. F3에서 대기 상태 책임을 A21로 이관. Phase 1-b에 A21 추가(공수 6일). |
| **1.0** | 2026-09-08 | **단일 문서(525줄) → 주제별 분할.** 에이전트 팀 작업 시 필요한 문서만 읽게 하기 위함. `PORTFOLIO_SPEC.md`는 내용을 비우고 **허브**(문서 지도 + 역할별 읽기 가이드 + 작업 규칙)로 전환 — 기존 경로 참조가 깨지지 않도록 파일명은 유지. 신설: `context.md` · `identity.md` · `roadmap.md` · `decisions.md` · `CHANGELOG.md` · `research/{benchmark-golmok,user-journey}.md` · `design/{emotional-design,design-system}.md` · `specs/{README,A-product-ux,B-data,C-reliability-ops,E-docs,F-quality}.md`. **D 그룹은 C에 편입**(결정 ②로 D3만 남음). 진행 상태는 `roadmap.md` 한 곳으로 일원화하고, 디자인 구현 값은 `design/design-system.md` 한 곳으로 일원화. `design-system.md`에 **치환 대응표**(현재 하드코딩 → 토큰) 추가 — A15를 그 문서만 보고 작업할 수 있게. E1·E5를 Phase 1 이월분으로 명시하고 Phase 1-b 공수를 3~4일 → 5~6일로 조정. |
| 0.8 | 2026-09-08 | **정체성(Identity & Goal) · 디자인 시스템 신설.** 포지셔닝을 "지표를 대신 읽어주고, 모르는 건 모른다고 말하는 상권 분석"으로 확정 — 데이터 양·정확도로는 골목상권을 못 이기므로 유일하게 이길 수 있는 축. Primary Goal = 주제 리포트 획득(A14), **Secondary Goal = 원본 대시보드 이탈**(신뢰 루프 완성이므로 이탈이 성공 신호). 성격 3형용사 `침착한·솔직한·정확한`. 역할별 팔레트·타입 스케일·4px 간격·라운드·그림자 원칙 정의. **타입 위계 붕괴 실측** — `font-size` 11종이 6px 범위에 몰려 `h1`↔`h2` 차이 2.4px, 핵심 수치가 `h1`보다 작음. **A19 신규**(증감에 초록/빨강 금지 — 매출 데이터가 없어 선악 판단 불가, 목동·대흥동 반례) · **A20 신규**(아이덴티티 적용). A15를 색+타입+간격+라운드 전체로 확장. 서비스 이름은 미결 ⑥으로 열어둠(추천: `상권 브리핑`). |
| 0.7 | 2026-09-08 | **감성 디자인 신설** — 목표 감정을 "자신감"이 아닌 **"정직한 신뢰"** 로 정의(데이터에 매출·유동인구가 없으므로 확신 연출은 거짓말이 됨). 키워드 `신뢰(주) × 차분함(톤) × 따뜻함(채팅 칸 한정)`, 에너지·럭셔리·플레이풀 배제. 현재 화면 진단 6건 — `#2563eb` 5중 역할 / 다크 임베드 톤 분열 / 무채색 AI 버블 / **한계 고지가 회색이라 최대 차별점이 시각적으로 가장 약함** / **Pretendard 미로드(실측 결함)** / `tabular-nums` 미적용. **A15~A18 신규**, 한계 고지 톤은 A12에 흡수. A16만 결함이라 Phase 1-b, 나머지는 Phase 2. |
| 0.6 | 2026-09-08 | **사용자 여정 분석(Entry→Retention) 신설** — 골목상권 "나도 곧 사장" 경로를 끝까지 밟아 6단계로 분해, 단계별 우리 구멍 진단(성숙도 표 포함). 정보 서비스에 맞게 `Decision=한계 고지` / `Action=산출물`로 치환. **A14(주제 리포트 CTA) 신규** — Action 단계의 종착점 부재를 메움. A12에 한계 고지 병기 추가, A13을 Retention 관점으로 재정의. Phase 1-b를 "사용자 여정 보강"으로 확장. |
| 0.5 | 2026-09-08 | A7(주제 전환 피드백)·A8(토큰 표시) 구현 반영. **벤치마킹(서울시 골목상권) 신설** — 페르소나 진입·질문 마법사·TOP 10 랭킹·강제 온보딩·기준 시점 노출·분석 저장 패턴 분석. 여기서 A9~A13 도출, A5는 A9·A10으로 대체. **Phase 1-b** 마일스톤 신설. |
| 0.4 | 2026-09-08 | **주제 상세를 자체 막대 → 실제 대시보드 패널 임베드로 교체.** 사유: 시각화 자산을 직접 활용하고 싶다(사용자). 대시보드에 "섹션만 보기" 모드(A2a) 신설 — 전체가 아니라 주제에 맞는 패널 하나만 렌더 + 셀렉트 프리셋. **시각화를 이중 관리하지 않게 됨.** |
| 0.3 | 2026-09-08 | **중앙 패널 재설계**: iframe 전체 임베드(A2) → 분석 주제 버튼 + 상세 뷰. 사유: 592px 칸에 1180px 대시보드가 안 맞아 가시성 낮음. A3a(크로스오리진 postMessage) 폐기·revert, A3b는 `{topic}` 단일 컨텍스트로 단순화. 전체 대시보드는 새 탭 딥링크로 유지. 주제 단일 선택. |
| 0.2 | 2026-09-08 | 열린 질문 5건 결정 반영: iframe 임베드 확정 / 인증(D1·D2) 제외 / 실데이터 전량 적재(B2 L→L+) / 커스텀 도메인 비범위 / 히스토리 보존 정책 없음. Phase 1 착수. |
| 0.1 | 2026-09-08 | 최초 작성 — 검토용 초안 |
